---
import "../../styles/global.css";
import data from "../../config/phone_messages.json";

const phone = data.phone_number;
const demo = encodeURIComponent(data.demo);
const chatbots = encodeURIComponent(data.chatbots);
---

<section
  class="relative min-h-screen flex items-center justify-center overflow-hidden"
>
  <div class="container mx-auto px-6 text-center z-20">
    <h1 class="text-4xl md:text-6xl font-bold mb-4 animate-on-scroll">
      Atiende <span class="gradient-text">1000 clientes</span> simultáneamente en
      WhatsApp <span class="gradient-text">sin contratar personal</span>
    </h1>
    <p
      class="text-lg md:text-xl max-w-3xl mx-auto text-accent mb-8 animate-on-scroll delay-100"
    >
      Chatbot con IA que responde consultas, agenda citas, procesa pedidos y
      cierra ventas 24/7. Tu equipo finalmente puede enfocarse en lo importante.
    </p>
    <div class="flex justify-center space-x-4 animate-on-scroll delay-200">
      <a href={`https://wa.me/${phone}?text=${demo}`} target="_blank" class="btn btn-primary">🚀 Probar Demo Ahora</a>
      <a href={`https://wa.me/${phone}?text=${chatbots}`} target="_blank" class="btn btn-secondary">💰 Ver Precios</a>
    </div>
    <div
      class="flex mt-8 gap-8 justify-center flex-wrap text-sm text-accent animate-on-scroll delay-300"
    >
      <div>✓ Responde en &lt;1 minuto</div>
      <div>✓ Sin límite de conversaciones</div>
      <div>✓ Entrenado con tu info exacta</div>
    </div>
  </div>
</section>
