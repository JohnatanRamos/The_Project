---
import "../../styles/global.css";
import data from "../../config/phone_messages.json";

const phone = data.phone_number;
const online_stores = encodeURIComponent(data.online_stores);
const includes_online_stores = encodeURIComponent(data.includes_online_stores);
---

<section
  class="relative min-h-screen flex items-center justify-center overflow-hidden"
>
  <div class="container mx-auto px-6 text-center z-20">
    <h1 class="text-4xl md:text-6xl font-bold mb-4 animate-on-scroll">
        Tu <span class="gradient-text">Tienda Online</span> Vendiendo 24/7
    </h1>
    <p
      class="text-lg md:text-xl max-w-3xl mx-auto text-accent mb-8 animate-on-scroll delay-100"
    >
    Creamos tu E-commerce completo en Shopify: diseño que convierte, pagos seguros, envíos automáticos y capacitación incluida. Sin complicaciones técnicas.
    </p>
    <div class="flex justify-center space-x-4 animate-on-scroll delay-200">
      <a href={`https://wa.me/${phone}?text=${online_stores}`} target="_blank" class="btn btn-primary">🚀 Solicitar Cotización</a>
      <a href={`https://wa.me/${phone}?text=${includes_online_stores}`} target="_blank" class="btn btn-secondary">💰 Explorar beneficios</a>
    </div>
    <div
      class="flex mt-8 gap-8 justify-center flex-wrap text-sm text-accent animate-on-scroll delay-300"
    >
      <div>✓ Entrega en 1-3 semanas</div>
      <div>✓ Diseño personalizado</div>
      <div>✓ Capacitación incluida</div>
    </div>
  </div>
</section>